# DBはPostgreSQL想定。ローカルはDocker等で用意する（docker-compose.yml）。
DATABASE_URL="postgresql://user:password@localhost:5432/hanamask"

# ローカルAPIサーバーのポート（省略時 3001。next devが /api/* をここへプロキシする）
# API_PORT="3001"

# Stripe（利用する場合）。カード情報は自社で保持しない。
STRIPE_SECRET_KEY=""
STRIPE_WEBHOOK_SECRET=""
