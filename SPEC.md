# SPEC: エージェントへの登録を「一度きり」にする（T45）

## Part 1: 利用者向け

### 何を・なぜ

いまの案内は「**エージェントに次のプロンプトを実行してください**」です。これは**使うたびの操作**であり、利用者が毎回それを意識する必要があります。**セットアップは意識してよいが、利用は無意識でありたい** — この切り分けができていません。

本来は、エージェントの設定に一度だけ登録すれば、以後は忘れてよいはずです。案内すべきものは「実行するプロンプト」ではなく「**設定に登録する一行**」です。

調べたところ、案内には具体的な問題が3つありました。

1. **入口の手順がプロンプト実行になっている。**登録ではないので、次回また同じことをすることになります
2. **登録コマンドに `--scope user` が付いていません。**このままだと**そのプロジェクトの中だけの登録**になり、別のフォルダで作業を始めるとまた登録が必要です。「一度きり」になっていません
3. **登録してもすぐには使えないことが書かれていません。**エージェントは起動時に接続先を読むため、**登録後にエージェントを立ち上げ直す**まで反映されません（この会話で実際に起きました）

### 画面イメージ・操作フロー

**設定画面に「AIエージェントから使う」という項目を追加します。**

- **登録コマンドをそのままコピーできるボタン**を置きます。押すとクリップボードに入ります
- コマンドの下に、**登録したあとエージェントを立ち上げ直す必要がある**ことを書きます
- Claude Code 以外のエージェント向けに、**設定ファイルへ貼る形式（JSON）もコピーできる**ようにします
- 現在の接続先（`http://127.0.0.1:39217/mcp`）も表示します。ポートを変えている場合は、その値が出ます

📸 設定画面の「AIエージェントから使う」

**READMEの手順2を書き換えます。**

- 「プロンプトを実行する」→「**エージェントに登録する**（一度だけ）」
- 登録コマンドに `--scope user` を付け、**どのフォルダで作業していても使える**ようにします
- 登録後にエージェントを立ち上げ直す旨を明記します

### 受け入れ条件

- [ ] 設定画面に、登録コマンドをコピーできるボタンがある
- [ ] コピーされる内容が `--scope user` を含み、**そのまま貼って実行できる**
- [ ] 接続先のポートを変えている場合、コピーされる内容にもその値が反映される
- [ ] 設定ファイルに貼るJSON形式もコピーできる
- [ ] **登録後にエージェントの再起動が必要**であることが、画面とREADMEの両方に書かれている
- [ ] READMEの手順2が「プロンプトを実行する」ではなく「登録する」になっている
- [ ] 実際に案内どおり登録して、エージェントから hanamask のツールが見える

### 未決定・要確認事項

1. **対応するエージェントの範囲。**Claude Code のコマンドと、汎用のJSON形式の2つを出す案にしています。特定のエージェント専用の記述を増やすと、追随できなくなったときに嘘になるためです。他に明示したいものがあれば教えてください。
2. **接続先は変更しません。**ポート `39217` を変えると、既に登録済みの利用者の設定が壊れます（T45の禁止事項）。
3. **hanamask が利用者の設定ファイルを自動で書き換えることはしません。**配るのは文面までで、書き込むかは利用者が決めます。

---

## Part 2: AI用（実装セット定義）

### 前提となる既存実装（読み取りのみ）

| 場所 | 現状 |
|---|---|
| `README.md` 「2. 利用しているAIエージェントにて下記のプロンプトを実行する」 | プロンプトを提示している。**登録ではない** |
| `README.md` 「Claude Code からつなぐ」 | `claude mcp add --transport http hanamask http://127.0.0.1:39217/mcp`。**`--scope user` が無い** |
| `src/main/mcp/server.ts` | ポートは `HANAMASK_MCP_PORT` で上書きでき、既定は 39217 |
| `src/renderer/components/StartupSettings.tsx` | 設定画面のセクションの書き方（T44で追加） |
| `src/shared/preload-api.ts` | `AppSettings` と `readAppSettings` / `saveAppSettings` |

### 実装セット

**セット A: 接続情報を画面へ渡す**

- 目的: 受け入れ条件「ポートを変えている場合、コピーされる内容にも反映される」
- 触ってよいファイル: `src/main/mcp/endpoint.ts`（新規。実効ポートとURLを組み立てる純粋関数）、`tests/main/mcp/endpoint.test.ts`（新規）
- 依存する既存ファイル（読み取りのみ）: `src/main/mcp/server.ts`（ポートの決め方を合わせる）
- 内容: `resolveMcpEndpoint(env)` が `{ port, url }` を返す。**`server.ts` の既定値と食い違わないこと**をテストで固定する

**セット B: 設定画面の「AIエージェントから使う」**

- 目的: 受け入れ条件のうち、コピーボタン・JSON形式・再起動の案内
- 触ってよいファイル: `src/renderer/components/AgentSetup.tsx`（新規）、`tests/renderer/AgentSetup.test.tsx`（新規）
- 依存する既存ファイル（読み取りのみ）: `src/renderer/components/StartupSettings.tsx`（見た目をそろえる）、`src/shared/preload-api.ts`
- 内容: 接続先URLを受け取り、コマンドとJSONを組み立てて表示する。コピーは `navigator.clipboard`。**組み立て部分は純粋関数に切り出す**（テストしやすくするため）

### Phase 4 統合ゲートでのみ編集するファイル

- `src/main/index.ts` / `src/preload/index.ts` / `src/shared/preload-api.ts` — 接続情報を渡すIPC
- `src/renderer/App.tsx` — 設定画面への組み込み
- `README.md` — 手順2の書き換えと `--scope user` の追加

### 並列グループ宣言

| グループ | セット | 同時実行 |
|---|---|---|
| 1 | A, B | **可**（触るファイルが重複しない） |

### 完了条件

- `npm test` / `npm run test:e2e` / `npm run lint` / `npm run typecheck` / `npm run build` が緑
- `npm run check:readme` が緑（READMEを触るため）
- **壊して確認する**: `--scope user` を落とすとテストが落ちること、ポート上書き時にURLが追随しなくなるとテストが落ちることを実測する
- 📸 設定画面のスクリーンショットを取得する
