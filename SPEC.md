# SPEC: エージェントが頼まれなくても書き・読む状態を作る（T46）

## Part 1: 利用者向け

### 何を・なぜ

**ツールが一覧に並んでいても、AIエージェントは明示的に頼まれない限り使いません。**このプロジェクト自身が実例で、開発を担当しているセッションはMCPが繋がる状態でも数日間まったく書き込みませんでした。目の前の作業を進めることに最適化した結果、記録先として常にリポジトリ内のファイルを選んでいたためです。

これは hanamask の作りの問題ではなく、**エージェントに「書く理由」が無かった**という問題です。効くのは利用者側の**常設指示** — 利用者が普段使っているエージェントの設定（Claude Code なら `CLAUDE.md`）に「作業の記録は hanamask に書く」と一行入れておくことです。これがあると、頼まなくても書きます。無ければ、どれだけツールが揃っていても書きません。

したがって hanamask が配るべきものは、**利用者が自分のエージェント設定に貼る文面**です。T45で配った登録コマンドと同じ考え方で、アプリ内にコピーボタン付きで置きます。

**あわせて「読む理由」も作ります。**書いたものが次に効かなければ、書く習慣は続きません。いまツールの説明文は、`list_tags` だけが「先に呼んで既存のタグを再利用しなさい」と促していて、`search_notes` は検索のやり方を述べるだけです。**作業を始める前に過去の経緯を引く**よう促す一文を足します。過去の決定が引けると分かれば、エージェントは**次の自分のために**書くようになります。

### 画面イメージ・操作フロー

設定画面の「AIエージェントから使う」（T45で追加済み）に、**もう一段追加します。**

- 「**エージェントに書く習慣を持たせる**」という項目
- 貼り付け用の文面と、**コピーボタン**
- どこに貼るかの案内（Claude Code なら `~/.claude/CLAUDE.md`、他のエージェントなら相当する常設指示の置き場）
- 文面は**そのまま貼れる完成形**にします。利用者が書き足す前提にしません

配る文面の骨子は次の4点です。

1. **書く**: 作業の区切り、方針や仕様の決定、調べて分かったこと、次に持ち越すこと
2. **読む**: 作業を始める前に検索して過去の経緯を引く
3. **ツールが見えないときは書かない**（アプリが動いていなければMCPも動いていない）
4. リポジトリ内の作業ログがある場合はそちらが正。hanamaskは**そこに残らない経緯・判断**を拾う

📸 設定画面の「エージェントに書く習慣を持たせる」

### 受け入れ条件

- [ ] 設定画面に、常設指示の文面をコピーできるボタンがある
- [ ] コピーされる文面が**そのまま貼って使える完成形**である（書き足しを前提にしない）
- [ ] どこに貼るかの案内が画面に書かれている
- [ ] `search_notes` の説明文に、**作業を始める前に引くよう促す一文**が入っている
- [ ] `create_note` / `create_task` の説明文が、**何を書くべきか**（決定・経緯）を示している
- [ ] READMEにも常設指示の案内がある
- [ ] ツール説明文を変えても、READMEのツール一覧との整合（`npm run check:readme`）が保たれている

### 未決定・要確認事項

1. **文面の最終形は管理者の確認を経てから配ります**（T46の停止条件）。利用者のエージェント設定に入るもので、hanamaskの外に影響するためです。**このPCの `~/.claude/CLAUDE.md` には既に同等の文面を入れており（2026-08-17、管理者の指示による例外）、実際に機能することは確認済みです。**配布する文面はこれを一般化したものにします。
2. **hanamask が利用者の設定ファイルを自動で書き換えることはしません。**配るのは文面までで、貼るかどうかは利用者が決めます（T45と同じ線引き）。
3. **効果の測り方。**「書くようになったか」をどう判定するかは、T47（使われていないと気づける状態）の指標に委ねます。本タスクでは測定機構を作りません。

---

## Part 2: AI用（実装セット定義）

### 前提となる既存実装（読み取りのみ）

| 場所 | 現状 |
|---|---|
| `src/main/mcp/tools.ts` の `TAGS_SCHEMA` | `Call list_tags first and reuse an existing tag…` と**先に呼ぶよう促している唯一の例**。書き方の手本にする |
| 同 `searchNotesTool` | `Search notes whose title or body contains the query. An empty query returns all.` — **やり方だけで動機が無い** |
| 同 `createNoteTool` | `Create a note in the local hanamask database. Tag it (see tags)…` — 何を書くべきかは示していない |
| `src/renderer/components/AgentSetup.tsx` | T45で追加。コピーボタンと純粋関数（`buildClaudeCodeCommand` 等）の書き方を踏襲する |
| `scripts/check-readme-tools.mjs` | READMEのツール一覧と実装の一致を検査する。**ツール名を変えなければ説明文の変更は影響しないはずだが、着手時に確認すること** |

### 実装セット

**セット A: ツール説明文の見直し**

- 目的: 受け入れ条件のうち「`search_notes` に着手前の参照を促す一文」「`create_note` / `create_task` が何を書くべきかを示す」
- 触ってよいファイル: `src/main/mcp/tools.ts`、`tests/main/mcp/tool-descriptions.test.ts`（新規）
- 依存する既存ファイル（読み取りのみ）: `scripts/check-readme-tools.mjs`、`README.md`
- 内容: 説明文を書き換える。**テストは文言そのものを固定するのではなく、「着手前に引くことを促す語が含まれる」程度の粒度**にする（一字一句を固定すると、改善のたびにテストを直すだけの作業が発生する）

**セット B: 常設指示の配布UI**

- 目的: 受け入れ条件のうちコピーボタン・貼り先の案内
- 触ってよいファイル: `src/renderer/components/StandingInstruction.tsx`（新規）、`tests/renderer/StandingInstruction.test.tsx`（新規）
- 依存する既存ファイル（読み取りのみ）: `src/renderer/components/AgentSetup.tsx`
- 内容: 文面を**定数として1箇所に置き**、コピーボタンで渡す。文面の組み立てが要る場合は純粋関数にする

### Phase 4 統合ゲートでのみ編集するファイル

- `src/renderer/App.tsx` — 設定画面への組み込み
- `README.md` — 常設指示の案内
- `docs/TASKS.md` / `docs/html/TASKS.html` — ステータス更新

### 並列グループ宣言

| グループ | セット | 同時実行 |
|---|---|---|
| 1 | A, B | **可**（触るファイルが重複しない） |

### 完了条件

- `npm test` / `npm run test:e2e` / `npm run lint` / `npm run typecheck` / `npm run build` が緑
- **`npm run check:readme` が緑**（ツール説明文を触るため）
- 📸 設定画面のスクリーンショットを取得する
- **壊して確認する**: 促しの一文を消すとセットAのテストが落ちること、文面の定数を空にするとセットBのテストが落ちることを実測する
