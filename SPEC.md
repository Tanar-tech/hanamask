# SPEC: 使われていないことに気づける状態にする（T47）

## Part 1: 利用者向け

### 何を・なぜ

この一連のタスク（T44〜T47）で一番の問題だったのは、**使われていないことに誰も気づかなかった**ことです。hanamaskは動いていて、MCPも繋がっていて、それでも数日間まったく書き込まれませんでした。**気づいたきっかけは会話の中でたまたま話題に上ったこと**で、アプリを見て分かったわけではありません。

いまのホーム画面は、最近のノートとタスクを新しい順に並べます。**中身は出ますが、「最後に書かれたのがいつか」は分かりません。**3日前の記録が一番上にあっても、それが「3日前で止まっている」のか「昨日も書いたが今日はまだ」なのか、日付を読んで自分で計算するしかありません。

そこで**ホーム画面を開けば、記録が続いているか途絶えているかが一目で分かる**ようにします。

### 画面イメージ・操作フロー

ホーム画面の上部に、**一行の状態表示**を置きます。

**続いているとき**

> 今週 12 件 · 最後の記録は 2 時間前

**途絶えているとき**

> **8 日間、記録がありません** · 最後の記録は 8 日前

- 途絶えているときは色と太字で目立たせます。ただし**警告や叱責にはしません**（書かない日があるのは普通のことです）
- **まだ1件も無いとき**は「まだ記録がありません」とだけ出します。日数は出しません（インストール直後に「0日間」と出ても意味がないため）

**数え方**

- 数えるのは**ノートとタスクの合計**です。作成と更新の両方を「記録された」とみなします（既存のノートに追記するのも記録のうちなので）
- **ゴミ箱に入れたものは数えません**
- 「今週」は**直近7日間**です。曜日の区切りではありません（月曜に見たとき「今週2件」と出るより、「直近7日で2件」の方が実態に近いため）

📸 ホーム画面（記録が続いているとき）
📸 ホーム画面（途絶えているとき）

### 受け入れ条件

- [ ] ホーム画面に、最後の記録からの経過と直近7日間の件数が出る
- [ ] しばらく記録が無いときは、その旨が目立つ形で出る
- [ ] **記録が1件も無いときに、日数や0件といった無意味な表示にならない**
- [ ] ゴミ箱に入れたノート・タスクが件数に含まれない
- [ ] ノートとタスクの両方が数えられる
- [ ] 既存のノートを更新したときも「記録された」として扱われる
- [ ] エージェントがMCP経由で書いた直後、**手動リロードなしで**表示が更新される

### 未決定・要確認事項

1. **「途絶えている」と判断する日数。3日を提案します。**平日に作業していれば3日空くのは珍しく、一方で週末を挟んだ程度では出ません。この値はあとから変えられます。
2. **利用状況を外部に送りません。**集計はすべてローカルで完結します（`docs/REQUIREMENTS.md` §9のローカル完結の前提を崩さない）。
3. **連続日数やバッジのような仕掛けは作りません。**目的は「気づけること」であって、書かせることを競わせることではありません（T47の禁止事項）。

---

## Part 2: AI用（実装セット定義）

### 前提となる既存実装（読み取りのみ）

| 場所 | 現状 |
|---|---|
| `src/main/db/tags-repo.ts` | 集計クエリの書き方の手本。`deleted_at IS NULL` で除外し、行の形を型ガード（`isTagsRow`）で検査してから使っている |
| `src/renderer/components/Home.tsx` | `listNotes` / `listTasks` を読み、`onNotesChanged` / `onTasksChanged` で再取得する。`nowMs` を状態に持ち、取得のたびに取り直している（タイマー停止中のずれを避けるため） |
| `src/shared/preload-api.ts` | `AppSettings` / `McpEndpoint` と、それぞれの `read*` API。同じ形で足せる |
| `src/main/index.ts` | IPCハンドラの登録箇所。変更通知の配信（`emitNotesChanged` 等）もここ |

### 実装セット

**セット A: 集計クエリ**

- 目的: 受け入れ条件のうち「ゴミ箱を含めない」「ノートとタスクの両方」「更新も記録として扱う」
- 触ってよいファイル: `src/main/db/activity-repo.ts`（新規）、`tests/main/db/activity-repo.test.ts`（新規）
- 依存する既存ファイル（読み取りのみ）: `src/main/db/tags-repo.ts`（書き方を踏襲）、`src/main/db/db.ts`
- 内容: `readActivity(now: Date)` が `{ lastRecordedAt: string | null; recentCount: number }` を返す。`lastRecordedAt` は notes/tasks の `updated_at` の最大値（`deleted_at IS NULL`）。`recentCount` は直近7日間に `updated_at` が入る件数。**1件も無ければ `lastRecordedAt` は `null`**
- **`now` を引数で受けること。**現在時刻を内部で取ると、境界のテストが書けない

**セット B: 表示の組み立て（純粋関数）**

- 目的: 受け入れ条件のうち「1件も無いときに無意味な表示にならない」「途絶えが目立つ」
- 触ってよいファイル: `src/renderer/components/activity-summary.ts`（新規）、`tests/renderer/activity-summary.test.ts`（新規）
- 依存する既存ファイル（読み取りのみ）: `src/shared/preload-api.ts`
- 内容: `summarizeActivity({ lastRecordedAt, recentCount }, nowMs)` が表示用の値（文言と、強調するかの真偽値）を返す**純粋関数**。**Reactに触れない**ので境界を機械的に試せる。「途絶え」の閾値は名前付き定数（3日）

### Phase 4 統合ゲートでのみ編集するファイル

- `src/main/index.ts` / `src/preload/index.ts` / `src/shared/preload-api.ts` — 集計を渡すIPC
- `src/renderer/components/Home.tsx` — 表示の組み込みと、変更通知での再取得
- `tests/e2e/` — MCP経由で書いた直後に表示が変わることの確認
- `docs/TASKS.md` / `docs/html/TASKS.html`

### 並列グループ宣言

| グループ | セット | 同時実行 |
|---|---|---|
| 1 | A, B | **可**（触るファイルが重複しない） |

### 完了条件

- `npm test` / `npm run test:e2e` / `npm run lint` / `npm run typecheck` / `npm run build` が緑
- 📸 ホーム画面のスクリーンショットを2種類（続いている／途絶えている）取得する
- **壊して確認する**: `deleted_at IS NULL` を外すとセットAのテストが落ちること、1件も無いときの分岐を消すとセットBのテストが落ちることを実測する
