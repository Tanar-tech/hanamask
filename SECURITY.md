# セキュリティ

## 脆弱性を見つけたら

**公開のIssueには書かないでください。**GitHubの [Security Advisories](../../security/advisories/new) から非公開で報告してください。

hanamask は個人が開発しているアプリのため、専任のセキュリティ担当や応答時間の保証はありません。受け取った報告は確認しますが、返信までに時間がかかることがあります。

## このアプリが前提にしていること

hanamask は**1台のPCで完結する**ノート・タスクアプリです。サーバーもクラウド同期もアカウントもありません。以下は仕様であって、脆弱性ではありません。

### 同じPCを使える人は、あなたのデータを読めます

データは `userData` 配下のSQLiteファイルに**暗号化せずに**保存されます（APIキーだけは例外、後述）。ノート・タスクの本文、タグ、画像がここに入ります。**OSのユーザーアカウントが唯一の境界です。**PCを他人と共有している、あるいはディスクが暗号化されていない場合、ノートの中身は保護されません。

### MCPサーバーには認証がありません

hanamask は起動中、`127.0.0.1` の固定ポートでMCPサーバーを動かします。**このサーバーは接続元を認証しません。**同じPC上で動くプロセスは、あなたのAIエージェントであれ他の何かであれ、**ノート・タスクの全読み書き（削除を含む）ができます。**

これは「利用者が普段使っているAIエージェントが、設定なしで即座に繋がる」という設計を優先した結果です。認証を必須にすると、エージェントごとに鍵を配る手間が発生します。

ただし、**ブラウザ経由で外部のWebサイトから叩かれることは防いでいます**（DNSリバインディング対策を有効化し、`Host` と `Origin` を許可リストで検証します）。防いでいないのは、**同じPC上で動く別のプログラム**からのアクセスです。

**したがって、信頼していないプログラムを動かしているPCでは hanamask を使わないでください。**

### 本文は信頼できない入力として扱っています

ノート・タスクの本文は**AIエージェントが書き込みます。**エージェントは読んだWebページや他人のリポジトリの内容をそのまま本文にすることがあるため、**本文の中身は攻撃者が制御しうる**前提で実装しています。

- 本文はReact要素として描画します。**`dangerouslySetInnerHTML` は使いません**
- HTMLの直接埋め込みを許可していますが、`<script>` / `<iframe>` / `javascript:` リンク / `onerror` 等のイベントハンドラ属性は描画時に除去します
- **`rehype-raw` → `rehype-sanitize` の順序が安全性の要です。**逆にすると生HTMLがサニタイズを素通りします。この順序を変更しないでください（`src/renderer/components/MarkdownBody.tsx`）
- 装飾のため `style` **属性**は許可していますが、`<style>` **タグ**は描画せず捨てます。タグ内のCSSはドキュメント全体に効くため、アプリのUIを覆い隠す偽装ができてしまうためです
- `style` 属性で `position: fixed` を使われてもアプリ全体を覆えないよう、本文の描画領域に `contain: paint` を掛けて閉じ込めています
- Content Security Policy を設定しており（本番では `script-src 'self'`）、上記のサニタイズと合わせて多層の防御にしています

### APIキーの扱い

**現在、APIキーを新たに保存する経路はありません。**AIチャットは動作しないため、2026-08-10 に画面から取り除きました（実装は将来のために残してあります）。

過去に保存した鍵がある場合、それは OSの資格情報ストア（Electron の `safeStorage`）で暗号化された状態で `userData` 配下に残っています。**画面から消せなくなっているので、不要なら `chat-settings.json` を直接削除してください。**

チャットを戻す場合の扱いは以下のとおりです（実装は変えていません）。

- `safeStorage` で暗号化して保存し、**暗号化が利用できない環境では保存を拒否**します。平文で書き出すことはしません
- **書き出し（バックアップzip）にAPIキーは含まれません**
- 画面に返すのは末尾4文字だけです

### バックアップの取り込みは破壊的です

取り込みは既存のノート・タスク・画像を**置き換えます。**実行前の状態は自動でzipに退避され、その保存先が画面に表示されます。取り込むzipは、**自分で書き出したものだけ**にしてください。他人から受け取ったzipを取り込むと、その中身であなたのデータが置き換わります。

## 対象外

以下はこのアプリの設計上、そもそも守る対象にしていません。

- 複数ユーザー・権限分離（1人で使う前提です）
- ネットワーク越しのアクセス（`127.0.0.1` 以外で待ち受けません）
- 保存データの暗号化（APIキーを除く）
- サービス拒否（DoS）・リソース枯渇

## 対応しているバージョン

最新のリリースのみです。古いバージョンへの修正の backport は行いません。
