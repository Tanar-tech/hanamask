# 変更履歴

このファイルは [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) の形式に従い、バージョンは [セマンティック バージョニング](https://semver.org/lang/ja/) に従う。

このアプリの「破壊的変更」は、MCPツールの引数・戻り値が変わること、および**古いバージョンで書き出したバックアップが取り込めなくなること**を指す。DBスキーマの変更そのものは、[docs/MIGRATIONS.md](docs/MIGRATIONS.md) の規約により既存データを保ったまま追従するため破壊的変更に数えない。

**1.0.0 以降は、上記の破壊的変更をメジャーバージョンの更新でのみ行う。**

## [Unreleased]

## [2.0.0] — 2026-08-20

**ローカルLLMをアプリに組み込み、ノート・タスクを「意味」で探せるようにした。**推論エンジン（node-llama-cpp、CPU版）と日本語向け埋め込みモデル（cl-nagoya/ruri-v3-70m、Apache-2.0、GGUF Q8_0 約73MiB）をインストーラーに同梱する。**完全にローカルで動き、記録の内容は外部へ送られない。APIキーも不要。**

メジャーバージョンを上げたのは、配布物の性格が変わるため（インストーラーが約126MB→約196MBに増え、ネイティブの推論ランタイムとモデルを含むようになる）。MCPツールの既存の引数・戻り値、バックアップの互換性に破壊的変更は無い。

### 追加

- **MCPツール `semantic_search_notes` を追加した。**自然文で問い合わせると、言葉が一致しなくても意味の近いノート・タスクが近い順に返る。エージェントが作業前に過去の経緯を引くとき、言い回しの違いによる取りこぼしを減らす。モデルが準備できていないときは空の結果と理由（`unavailable`）を返すので、そのときは従来の `search_notes` を使えばよい
- **検索結果画面に「意味が近い記録」欄を追加した。**キーワード検索の結果の下に、意味の近いノート・タスクが並ぶ
- **ノート詳細に「関連するノート」欄を追加した。**開いているノートに内容が近いノートが最大5件並び、クリックで開ける
- 意味検索の各行に**更新日を併記**した。その記録がいつのものかは、開くかどうかを決める材料になるため
- 索引（埋め込みベクトル）はノート・タスクの作成・更新の数秒後に裏で自動更新される。起動時は未索引・内容が変わった記録だけを埋める。モデルの読み込みに失敗しても、意味検索の欄が出ないだけで他の機能は通常どおり動く

### 変更

- インストーラーのサイズが約196MBになった（モデル約73MiB＋推論エンジンの同梱による）
- 開発者向け: モデルはリポジトリに含めず、`npm run fetch:model` が固定URLから取得して sha256 を検証する（不一致は破棄）

### セキュリティ

- 推論は完全ローカルで、`src/main/` が外部通信のコードを含まないことをテストで固定した（許可リストは localhost 待ち受けの MCP サーバーのみ）
- 同梱するネイティブバイナリは CPU 版のみ（CUDA/Vulkan/arm64 は配布物から除外）。モデル・エンジンのライセンス文を同梱し、NOTICE に帰属表示を載せた

## [1.0.1] — 2026-08-19

**アプリの機能に変更はない。**同梱するElectronランタイムの更新のみ。

### 修正

- **Electron を 43.3.0 → 43.4.0 に更新した。**上流の安定性修正が入る。特に **Windowsのログオフ・シャットダウン・再起動時のブラウザプロセスのクラッシュ**が直っており、**ウィンドウを閉じても通知領域で動き続ける**hanamaskはOS終了時の経路を通る機会が多いため、効く可能性がある。あわせてメニュー切り替え時のクラッシュ（高負荷時）も修正されている。破壊的変更は無い

## [1.0.0] — 2026-08-18

**hanamask をオープンソースとして公開した。**あわせて、作ったものが実際に使われるまでの段差を埋めた。

### 追加

- **ウィンドウを閉じてもアプリが終了しなくなった。**通知領域に残り続ける。**MCPサーバーはアプリの中で動いているため、閉じた瞬間にエージェントから見て hanamask が存在しなくなっていた。**「今日は開いておこう」と意識しないと使えない状態では、日常的に使われない
- **ログイン時の自動起動を選べるようにした。**既定はオフ（利用者の同意なくOSのログイン項目を書き換えないため）。自動起動で立ち上がったときはウィンドウを出さず通知領域だけに入る
- **設定画面に「AIエージェントから使う」を追加した。**登録コマンドと、他のエージェント向けのJSONをコピーできる。ポートを変えている場合はその値が入った状態で出る
- **設定画面に「エージェントに書く習慣を持たせる」を追加した。**エージェントの設定に貼る文面をコピーできる。**ツールが使える状態にしただけでは、エージェントは書かない**（このアプリの開発中に、MCPが繋がっているのに数日間まったく使われなかった）
- **ホーム画面に記録の状態を表示するようにした。**「今週◯件 · 最後の記録は◯時間前」、しばらく空いていれば「◯日間、記録がありません」。**使われていないことに気づけなかった**のが、この一連の変更の発端
- ホーム画面の「最近のノート・タスク」と検索結果にもタグを表示するようにした

### 変更

- **エージェントへの登録手順を「一度きり」に直した。**READMEが案内していたのは毎回打つプロンプトで、登録コマンドには `--scope user` が無く、**そのコマンドを実行したプロジェクトの中だけの登録**になっていた。**登録後にエージェントの再起動が必要**であることも明記した
- **MCPツールの説明文に、読む動機と何を書くべきかを入れた。**`search_notes` は検索のやり方しか書いておらず、着手前に過去を引く理由が無かった。`create_note` / `create_task` には「決定は理由ごと本文に残す」を加えた
- ノート・タスクの一覧を1ページ20件で区切り、ページングできるようにした
- タスク画面でカンバンを一覧より上に出すようにした

### 修正

- **`mermaid` を 11.16.1 へ更新し、CSS injection の脆弱性を解消した。**本文はエージェントが書くため、図のソース経由で到達しうる箇所だった

### セキュリティ

- **リポジトリを公開し、`main` にブランチ保護を適用した。**CI（Ubuntu / Windows / semgrep）が緑でなければマージできない。force push とブランチ削除を禁止し、管理者にも適用している

## [0.3.0] — 2026-08-10

### 追加

- **`list_tags` を追加した。**使われているタグと件数を返す。エージェントは過去に自分が何と名付けたかを覚えていないため、これが無いと同じ案件に「プロジェクトA」「project-a」のような別名が付き、グループとして機能しなくなる
- **タスクにもタグを持たせた。**これまでタグはノートにしか無かった
- **一覧にタグを表示するようにした。**詳細画面にしか出ておらず、一覧を見てもどの記録がどの案件のものか分からなかった
- **タグごとに分けて並べられるようにした。**1つの記録が複数のタグを持つなら、それぞれの見出しの下に出る。どちらか一方にしか出さないと「この案件にも属している」ことが見えなくなる
- **タグで絞り込めるようにした。**「あるノートがプロジェクトAに所属し、プロジェクトBには所属していない」ことを選ぶだけで判別できる。複数選んだときは**どれかに一致**（OR）

### 変更

- タグの絞り込みとグループ表示をE2Eで検証するようにした。**画面の結線なので、単体テストだけでは実際に効いているか分からない**

### 修正

- **取り込み時のDB置き換えが途中で中断すると、次の起動で開けなくなりえた。**直接上書きしていたうえ `-wal` 等も消していたため、SQLiteの自動復旧も効かなかった。画像と同じく、一時ファイルへ書き切ってから置き換えるようにした
- **起動に失敗したとき、画面に何も出ていなかった。**DBが壊れているとアプリが黙って立ち上がらないだけで、退避したzipがあることにも辿り着けなかった。理由とデータの場所・退避先を出すようにした

## [0.2.2] — 2026-08-10

### 修正

- **存在しないノート・タスクへのリンクが黙って作られていた。**`links` テーブルに外部キーが無く、両端の存在も確かめていなかったため、エージェントがidを打ち間違えると「何も指さないリンク」が残り、利用者からは開けないリンクにしか見えなかった。張る時点で断るようにした
- **E2Eが利用者の実データのディレクトリへ書き込んでいた。**`HANAMASK_DB_PATH` でDBだけを別にしても、画像・バックアップ・設定は `userData` を指したままだった。取り込みは画像ディレクトリを丸ごと差し替えるため、そのままでは利用者の画像を消しうる状態だった

### 変更

- 検索をE2Eで検証するようにした。**本文にしかない語で引けること**と、**削除したノートが出てこないこと**を見る
- カンバンでの移動をE2Eで検証するようにした。**HTML5のドラッグ&ドロップはPlaywrightのマウス操作では発火しない**ため、`dataTransfer` を持つイベントを組み立てて送っている
- 編集履歴からの復元をE2Eで検証するようにした。**復元は今の本文を古い内容で上書きする**ため、復元そのものも履歴に残る（＝やり直せる）ことまで確認する
- 書き出しと取り込みをE2Eで検証するようにした。**データを失ったときの唯一の復旧経路**であり、取り込みは既存のデータを丸ごと置き換えるため

## [0.2.1] — 2026-08-10

### 修正

- **`update_note` が本文を保存していなかった。**タイトルとタグは更新されるのに本文だけ古いまま残り、エージェントがノートの本文を書き換えられなかった（0.2.0 に含まれる回帰）
- E2EをCIで実行するようにした。**4本あるのに一度も自動実行されておらず、上記の回帰が単体テスト633件緑のまま2日間気付かれなかった**

- CIをWindowsでも回すようにした。**配布対象はWindowsなのに、これまで ubuntu でしか検証していなかった。**0.2.0 のリリースで初めてWindows上でテストが走り、パス区切りを直書きしたアサーションが落ちて気づいた

### 変更

- ゴミ箱の猶予日数の定数名を `NOTE_RETENTION_DAYS` から `TRASH_RETENTION_DAYS` に変えた。**実際にはノートとタスクの両方に使われており、名前が実態と食い違っていた**（値は30日のまま）

## [0.2.0] — 2026-08-10

### 追加

- **タスクを画面から削除し、ゴミ箱から復元できるようにした。**削除の仕組みはDBとMCPには既にあり、保持期間を過ぎたものは実際に消去されていた一方で、**画面には削除済みタスクを見る手段も戻す手段も無かった**（#113）
- タスクに本文を持たせた。ノートと同じく Markdown・HTMLの直接埋め込み・Mermaid図が使える（#103）
- タスク詳細からタイトルと本文を編集できるようにした。これまでステータスしか変更できなかった（#103）
- タスク一覧のカードに本文の抜粋を表示するようにした（#103）
- DBスキーマのマイグレーション機構を追加した。既存のデータを保ったまま列を追加できる（#103）
- タグを打つとWindowsインストーラーをビルドして GitHub Releases に添付するようにした

### 変更

- 詳細画面を開いている間、左レールの現在地がその種別（ノート／タスク）を指すようにした。エージェントが `open_task` で画面を切り替えると、直前に選んでいたセクションが選択されたまま残っていた
- ノート・タスクの一覧で、本文の抜粋から Markdown の記号を落として表示するようにした。`# 見出し` や ```` ```mermaid ```` がそのまま文字として出ていた（#103）
- 本文が空のノート・タスクの詳細画面に「本文はまだありません」を表示するようにした。以前は無地で、読み込み失敗と見分けが付かなかった（#103）

### 修正

- **バックアップの取り込みで画像が失われることがあった。**既存の画像を先に全削除してから書き込んでいたため、途中で失敗すると旧画像だけが消えていた（#103）
- **アプリを二重に起動するとマイグレーションが競合し、起動できないことがあった**（#103）

### セキュリティ

- **MCPサーバーに DNS リバインディング対策を入れた。**`Host` と `Origin` を検証していなかったため、利用者がブラウザで開いた悪意あるページからノート・タスクを操作されうる状態だった（#103）
- **本文中の `<style>` タグを描画しないようにした。**タグ内のCSSはアプリ全体に効くため、偽の確認ダイアログでUIを覆う偽装ができた。インラインの `style` 属性による装飾は今までどおり使える（#103）
- **本文の描画領域に `contain: paint` を掛けた。**`style` 属性の `position: fixed` でアプリ全体を覆えないようにするため（#103）

## [0.1.0]

- 最初の内部リリース。
